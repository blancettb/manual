{
  "name": "Redis cluster state isn't ok",
  "description": "Cluster is in inappropriate state",
  "type": "MANUAL",
  "tags": [
    "container",
    "docker",
    "podman",
    "cpu",
    "diagnostic",
    "watsonx"
  ],
  "fields": [
    {
        "name": "content",
        "description": "Content for manual action",
        "encoding": "base64",
        "value": " 1. Check the Redis logs for any error messages or warnings related to the cluster state. These logs can provide valuable information about the cause of the problem.
2. Verify that all nodes in the Redis cluster are running the same version of the software. If there is a difference in version, update all nodes to the latest version to ensure compatibility and avoid potential issues.
3. Inspect the Redis cluster configuration to ensure that it is set up correctly. This includes checking the node roles, data replication settings, and network configurations. Make sure that the configuration matches the desired state of the cluster.
4. Perform a "cluster reshard" operation to redistribute the data across the nodes in the cluster. This can help to resolve issues caused by uneven data distribution or misconfigured replicas.
5. If the issue persists, consider using RedisInsight, a third-party tool that provides advanced monitoring and troubleshooting capabilities for Redis clusters. RedisInsight can help identify and diagnose complex issues related to the cluster state.

By following these steps, you should be able to resolve the Redis cluster state isn't ok issue and ensure that your Redis cluster is operating smoothly.",
        "secured": false
    }
],
  "staticId": "containerCPUDebug",
  "metadata": {
    "ai": [
      {
        "algorithm": "watsonx",
        "events": ["gYV69gQWO6hNwmb3My25YFZpSS4"]
      }
    ]
  }
}